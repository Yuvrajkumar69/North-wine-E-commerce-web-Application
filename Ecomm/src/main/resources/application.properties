spring.application.name=Ecomm

# ── Database ──
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ecommerce}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ── Razorpay (Test Mode) ──
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_placeholder}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:rzp_secret_placeholder}

# ── Brevo Transactional Email ──
brevo.api.key=${BREVO_API_KEY:}
brevo.sender.email=${BREVO_SENDER_EMAIL:singhpunam5091@gmail.com}
brevo.sender.name=${BREVO_SENDER_NAME:North & Vine}

# ── App & Frontend URL ──
app.frontend.url=${FRONTEND_URL:http://localhost:5500}

# ── Store Contact Info ──
store.location=Durga City Center, Haldwani, Uttarakhand, India
store.phone=7500554902
store.email=singhpunam5091@gmail.com

# ── CORS ──
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:*}